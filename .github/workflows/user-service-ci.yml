name: User Service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "services/user-service/**"
      - ".github/workflows/user-service-ci.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "services/user-service/**"
      - ".github/workflows/user-service-ci.yml"

jobs:
  test:
    name: Test User Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Run tests
        working-directory: ./services/user-service
        run: mvn clean test -B

      - name: Generate test report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: User Service Test Results
          path: services/user-service/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true

  build:
    name: Build User Service
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Build with Maven
        working-directory: ./services/user-service
        run: mvn clean package -DskipTests -B

      - name: Build Docker image
        working-directory: ./services/user-service
        run: docker build -t user-service:${{ github.sha }} .

      - name: Save Docker image as artifact
        run: |
          docker save user-service:${{ github.sha }} | gzip > user-service-${{ github.sha }}.tar.gz

      - name: Upload Docker image
        uses: actions/upload-artifact@v4
        with:
          name: user-service-image
          path: user-service-${{ github.sha }}.tar.gz
          retention-days: 7
